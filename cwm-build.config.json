{
    "_comment": "All three manifests bump in lockstep on a cwmconnect release: the component, the bundled site module, and the bundled finder plugin. The component is the primary surface; the module + finder plugin only ship from this repo and have no independent release cadence. ARS categoryId/updateStreamId are placeholders — populate before first release (run `cwm-ars-list` against the CWM ARS site to discover IDs).",
    "_profile_comment": "Component-shaped versionTracking despite the package wrapper — cwmconnect owns its own versions.json + package.json on a single cadence. The substituteTokens.paths override drops 'language/' (cwmconnect has no top-level language/ dir; language files live under admin/language, site/language, modules/site/mod_birthdayanniversary/language, and plugins/finder/cwmconnect/language — all reached via the admin/, site/, modules/, plugins/ scan roots).",
    "profile": "component",
    "versionTracking": {
        "substituteTokens": {
            "paths": ["admin/", "site/", "modules/", "plugins/"]
        }
    },
    "extension": {
        "type": "package",
        "name": "pkg_cwmconnect"
    },
    "manifests": {
        "package": "build/pkg_cwmconnect.xml",
        "extensions": [
            { "type": "component", "path": "cwmconnect.xml" },
            { "type": "module",    "path": "modules/site/mod_birthdayanniversary/mod_birthdayanniversary.xml" },
            { "type": "plugin",    "path": "plugins/finder/cwmconnect/cwmconnect.xml" }
        ]
    },
    "build": {
        "command": "php build/build-package.php build",
        "outputGlob": "build/dist/pkg_cwmconnect-*.zip"
    },
    "ars": {
        "endpoint": "https://www.christianwebministries.org",
        "categoryId": 0,
        "updateStreamId": 0,
        "environments": ["50", "51", "60"],
        "tokenItem": "CWM ARS API Token",
        "tokenVault": "CWM",
        "itemDescription": "CWM Connect — Church Photo Directory component for Joomla 5/6, with a birthday/anniversary site module and Smart Search adapter for directory entries."
    },
    "github": {
        "owner": "Joomla-Bible-Study",
        "repo": "cwmconnect",
        "releaseBranch": "master"
    },
    "changelog": {
        "file": "build/cwmconnect-changelog.xml",
        "url":  "https://raw.githubusercontent.com/Joomla-Bible-Study/cwmconnect/master/build/cwmconnect-changelog.xml"
    },
    "dev": {
        "_comment": "Drives cwm-link / cwm-link-check / cwm-clean / cwm-verify against the developer's local Joomla install(s). Component admin/site/media + the bundled module + finder plugin are auto-derived from manifests.extensions[]; the internalLinks entry replaces the manual `ln -s` step previously documented in CLAUDE.md.",
        "internalLinks": [
            { "source": "cwmconnect.xml", "link": "admin/cwmconnect.xml" },
            { "source": "script.php",          "link": "admin/script.php" }
        ]
    }
}
